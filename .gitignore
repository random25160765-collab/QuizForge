# ---------------------------------------------------------------- 密钥
# 含 AI 密钥的本地配置，绝不入库
config/ai.local.json
# 本机跑服务时读的环境变量（端口、用量上限等，用 make env-init 生成）
/.env
api/.env

# ---------------------------------------------------------------- Python
api/.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# ---------------------------------------------------------------- 构建产物
# 前端产物。本地由 make web 生成；镜像里由 Dockerfile 的多阶段构建生成，
# 不依赖这个目录进版本库
api/web/
# 用户上传的附件：只在本机，绝不进版本库（内容是别人的文档，还可能有个人信息）
api/var/
# vendor/ 全部入库：离线也要能起（pyodide 约 76MB，含 numpy/scipy 预置包）
# 代码排版：JetBrains Mono 字体 + highlight.js（同上）

# ------------------------------------------------------------ 运行期数据
*.db
*.sqlite3

# -------------------------------------------------------- 外部参考资料（只读）
# reference/ 是指向 /mnt/f/Documents 的软链：手册、论文、教科书，几百 MB 级，
# 绝不入库。材料只读，进 git 的只能是题目。
/reference

# ------------------------------------------------------------------ 编辑器
.DS_Store
.idea/
.vscode/
*.swp

# 流水线的运行日志与缓存（2026-09-16）
pipeline/*.log
pipeline/*.out
pipeline/state.sqlite3*
__pycache__/
*.pyc

# 浏览器自测的产物（2026-09-17）
# playwright-cli 把控制台日志与页面快照写在**当前工作目录**，在仓库根跑一次就到这里。
# 它们是可重建的临时物，不该进版本库，也不该在 git status 里刷存在感。
/.playwright-cli/

# 流水线中间产物：知识空间已在数据库里，这些随时可重跑（2026-09-16）
maps/**/extract/
maps/**/figures/
maps/**/*.verify.json
maps/**/pack-*.json
maps/_attic/

# 题库物化的临时目录（权威在数据库，用完即删）
.bank-cache/

# 应用数据目录（local-first）：库文件、以后的笔记与资料索引都在本机，不进版本库
data/

# 打包产物：单文件可执行程序与 PyInstaller 的工作目录
build/dist/
build/.pyinstaller/

# 分享页产物（make share 生成，一条会话一个文件，几 MB）
share-*.html
