# 去账号与换内置引擎 · 改动清单（核对表）

> B 段开工前的扫面结果（2026-09-18，code-explorer 全仓扫描）。
> **改完之后拿它对账**：一条一条划掉，没划掉的就是漏改的。
> 两件事：**A 去账号**（多用户 → 单用户本地）、**B 换内置引擎**（PostgreSQL → SQLite）。
>
> ## 进度
>
> - **A 去账号：已做完**（2026-09-18）。做法与清单略有出入，记在这里：
>   - `routers/auth.py` / `security.py` 里的会话与 CSRF **整块删掉**、`deps.py` 重写成
>     "拿到本机用户"；但**依赖名沿用** `CurrentUser` / `AuthenticatedWriter` ——
>     9 个 router、40+ 处读取一行都不用改，`user_id` 那些过滤在单用户下恒真。
>   - 前端删 `login.html` / `landing.html`，`index.html` 改成直接进应用；
>     `boot.js` 去掉登录守卫，`api.js` 去掉 CSRF 与 `api.auth`。
>   - 测试：删 `test_auth.py` 与所有"多用户隔离 / CSRF / 未登录 401"用例；
>     各文件的 `_register` 变成"本机用户就绪"；**新增 autouse 夹具在同一个用户下清数据**
>     （原先的隔离是"每个用例注册一个新用户"顺带给的）。
>   - 实测：`/` → `/quiz.html` 直接进应用、`/login.html` 404、对话页不跳登录；
>     后端 183 项、前端 3170 断言全绿。
> - **B 换内置引擎：未开始**。下面的 B 节仍是待办。

---

## A. 去账号

### A.1 三处定义（删掉它们，其余都是"调用点"）

| 文件 | 位置 | 是什么 |
|---|---|---|
| `api/app/routers/auth.py` | 全文件（~245 行） | 端点：`POST /register`(119) · `/login`(155) · `/logout`(185) · `GET /me`(203) · `POST /password`(224)；另有限流 `_FAILED`(53) 与 `_start_session`(102) |
| `api/app/deps.py` | 全文件（~165 行） | `get_optional_user`(39) · `CurrentUser`(53) · `verify_same_origin`(66) · `require_csrf`(90) · `AuthenticatedWriter`(103) · `guard_write`(118) · `set_session_cookie`(127) · `set_csrf_cookie`(140) · `clear_session_cookies`(159) |
| `api/app/security.py` | 全部 | 口令哈希 / 会话令牌 / CSRF 令牌的底层函数 |

### A.2 每个 router 的「按用户过滤」（逐个划掉）

| 文件 | 行号 | 说明 |
|---|---|---|
| `routers/bank.py` | 52, 80 | 只是要求登录（读接口本不需要身份） |
| `routers/knowledge.py` | 41, 73, 111, 147, 199（`CurrentUser`）· 254（`Record.user_id`）· 293（`UserQuestion.user_id`） | 掌握度与"我的题单"参与打分 |
| `routers/graph.py` · `health.py` | — | **无鉴权**，不用改 |
| `routers/problem.py` | 137, 172, 240, 259, 284, 285, 376 | 记录归属 + 取本人 AI 配置/配额 |
| `routers/progress.py` | 41, 47-53, 66, 78-87, 92-93, 110, 116, 121-129 | 快照 / 同步 / 重置全部按 `user.id` |
| `routers/ai.py` | 51, 56-57, 120, 128, 194, 201, 220 | 配置 / 配额 / 用量 |
| `routers/mybank.py` | 22, 75, 79, 87, 95, 105, 122, 127, 143, 151 | 用户题单 |
| `routers/chat.py` | 67, 135-144(`_own_conversation`), 142, 204, 235, 255, 476, 485, 496, 506, 527, 560, 563, 571, 579, 599, 602, 621, 627, 638, 657, 660, 685, 740, 750, 781, 787, 811, 821, 826-827, 837, 876, 882, 899, 947, 971, 986, 1021, 1031, 1033 | **最密集的一处**（≈42 点）：会话归属断言 + 消息/附件归属 + AI 配置 |

### A.3 `models.py` 里带归属列的表

| 表 | 归属列 | 外键 |
|---|---|---|
| `users` | `id`(UUID PK) · `email` unique | — |
| `sessions` | `user_id` | `users.id` CASCADE |
| `user_questions` | `user_id` | CASCADE |
| `records` / `attempts` / `days` / `user_settings` / `ai_usage` | `user_id`（`records` / `days` / `user_settings` / `ai_usage` 里是主键的一部分） | CASCADE |
| `conversations` | `user_id` | CASCADE |
| `messages` | `conversation_id` + `user_id`（冗余）+ `parent_id` | 三条都 CASCADE |
| `attachments` | `user_id` + `message_id` | 两条都 CASCADE |

> `models.py` 里 `ondelete="CASCADE"` 共 24 处（85, 217, 250, 289, 334, 364, 381, 476, 479, 513, 516, 536, 566, 590, 593, 614, 617, 715, 751, 799, 806, 809, 858, 862），
> 其中**只有归属那几个是"多用户"留下的**，其余是业务外键（材料↔点↔边），换库后要 `PRAGMA foreign_keys=ON` 才生效。
> `SET NULL` 两处（182 `bank_version_id`、539 `concept_id`）是业务的，保留。

### A.4 前端

| 位置 | 说明 |
|---|---|
| `theme/runtime/boot.js` | 6（注释）· 107-114 `loginUrl()/toLogin()` · 215-216 `api.onUnauthorized(toLogin)` · 218-232 启动时 `auth.me()` 判登录 · 247-248 错误页"回到登录页"按钮 |
| `theme/runtime/api.js` | 27-29 `csrfToken()` · 69 写请求带 `X-CSRF-Token` · 91 / 192 401 → 跳登录 · **248-280 `api.auth.*` 整块** |
| `theme/login.html` | 383-386 `csrf()` · 388-406 / 434-445 登录与注册 · 448-454 打开即 `auth.me()` |
| `theme/landing.html` | 373-383 `auth.me()` 决定去 quiz 还是 login |
| `theme/shell.html` | 无需改（只有主题引导的注释） |
| `theme/runtime/ai.js` | 255 的 `401` 是「AI 密钥被拒」，**不是会话**，保留 |

---

## B. 换内置引擎（PostgreSQL → SQLite）

### B.1 四处 `pg_insert` / 咨询锁 / jsonb 原生 SQL（重点）

| 文件 | 位置 | 现状 → 改法 |
|---|---|---|
| `pipeline/dbstore.py` | 94-96 | `pg_advisory_xact_lock(hashtext(:key))` → `BEGIN IMMEDIATE` 事务 + 读最大值（单机串行够用） |
| `api/app/sync_ops.py` | 31（import）· 237-252（`pg_insert(Attempt)`）· 309-369（`pg_insert(Record)` + `func.least/greatest` + `cast`）· 378-417（`bump_days` 的 `with_for_update`）· 443-452（`apply_days_seed` 同） | `pg_insert` → SQLite `insert()`；`least/greatest` → `min/max`；删 `cast(...)`；`with_for_update` → `BEGIN IMMEDIATE` 内读改写 |
| `api/app/outline.py` | 155-158（`CAST(:path AS JSONB)` + `now()`）· 213（`IS NOT DISTINCT FROM`）· 225（`'[]'::jsonb`）· 279（`path @> CAST(... AS JSONB)`）· 284（`= ANY(:keys)` + `now()`） | JSON 存文本、应用层过滤包含关系、`IN` 展开、`CURRENT_TIMESTAMP` |
| `api/app/ai_gateway.py` | 308（import）· 310-334（`pg_insert(AiUsage).on_conflict_do_update`）· 331（`func.now()`） | 同 sync_ops 的改法 |

### B.2 其它方言写法（按文件）

| 文件 | 位置 | 写法 |
|---|---|---|
| `api/app/models.py` | 44, 51 | `JSONType = JSON().with_variant(JSONB(), "postgresql")` —— 被 **22 列**引用（107/129-132/171/178/219/257-260/303/306/344/369/383/436/545/824）→ 退化成纯 `JSON` |
| `api/app/graph.py` | 262 | `= ANY(:ids)` → `IN` 展开 |
| `api/app/routers/chat.py` | 497（`.distinct(...).order_by(...)` → PG `DISTINCT ON`）· 513-527（`ilike`） | 换窗口函数/子查询取每会话最后一条；`ilike` → `lower() LIKE lower()` |
| `api/app/tools.py` | 92-95 · 1113-1115（`cast(Concept.aliases, String).ilike(...)`）· 113（`ilike`） | 改应用层过滤 |
| `pipeline/dbstore.py` | 168-175 · 230-234 · 253-254 · 278 · 334-338 · 386-389 · 427-449 | `CAST(... AS JSONB)` · `now()` · `RETURNING` · `payload -> 'point'` |
| `pipeline/dbsync.py` | 45-50 · 58-62 · 69 · 76 · 83 · 92-94 · 104-106 · 226-227 | 同上（约 14 处） |
| `pipeline/promote.py` | 142-143 · 193-194 | `CAST(... AS JSONB)` + `now()` |
| `pipeline/fix.py` | 44 · 143-145 · 173 | `payload ->> 'point'` + `CAST` + `now()` |
| `pipeline/rework.py` | 40（`(payload ->> 'round')::int`）· 51 · 136 | JSON 取值 + 类型转换 |
| `pipeline/bankfile.py` | 60 · 103-104 · 109 · 120 · 174 | `= ANY(:st)` · `CAST(... AS JSONB)` · `ON CONFLICT` · `now()` |
| `pipeline/graph_build.py` | 243（`bool_or`）· 255 · 264 · 350 · 352 · 363 · 383 · 484 · 888 · 905 | `bool_or` → `max`；`CAST(... AS JSONB)`；`now()`；`= ANY(:ids)` |
| `pipeline/graph_log.py` | 122 · 148 | `updated_at = now()` |
| `pipeline/drive.py` | 63-66 | `COUNT(*) FILTER (WHERE ...)` → `SUM(CASE WHEN ...)` |
| `pipeline/coverage.py` | 54-55 | 同上（`FILTER`） |
| `pipeline/dispatch.py` | 334-340 | `jsonb_agg(DISTINCT jsonb_build_array(...))` + `'[]'::jsonb` → 应用层拼 JSON |
| `pipeline/store.py` | 94 | `BEGIN IMMEDIATE` —— 这是**任务表的 sqlite3**，正常，别误改 |

> 扫描确认**没有**用到的：`array_length`、`gen_random_uuid()`、`INTERVAL`、`width_bucket`、`unaccent`、`regexp_replace`、窗口函数（`hljs` 里的同名关键字只是语法高亮表）。
> `COALESCE` / `ILIKE`（SQLAlchemy 会降级）/ `func.distinct` / `COUNT(DISTINCT ...)` 基本可移植，**实测确认即可**。

### B.3 迁移目录（12 个文件里全是 PG DDL）

- `20260915_0621_initial_schema.py`：`postgresql.JSONB`（30/44-47/80/96/106/132/135-136/161）· `sa.Uuid()`（55/66/74-75/92/121/146-147/160）· `sa.text('now()')`（27/50/59/82/110-111/138/149/163）
- `20260915_0654_..._records_payload_and_sync_rev.py`：`JSONB` + `Uuid` + 注释「**直接重建表**，不做数据迁移」（PG 无法隐式转换 `timestamptz → BIGINT`）
- `20260915_0900_a1c4e0f27b93_...py`：`jsonb_build_object`（85/118）· `payload->>` / `->`（33/35/74/76/86-87/129-131）· `::bigint` / `::double precision` / `::date` / `::int` + `~` 正则（33/35）· `AT TIME ZONE 'UTC'`（45）
- 其余（`0704` · `0835` · `0904` · `0921` · `1210` · `1227` · `20260917_0800` · `1201` · `1500`）：`JSONB` / `Uuid` / `now()` / `ondelete`
- `alembic/env.py:64`：注释「PG 的 DDL 是事务性的」
- **建议**：既然改成单用户本地 SQLite，最干净的做法是**删掉整个 `api/alembic/`**，改由 `Base.metadata.create_all()` 建表（`conftest` 已经是这条路径）

### B.4 测试与环境

| 位置 | 说明 |
|---|---|
| `api/tests/conftest.py` | 25-27（PG DSN 默认值）· 36（派生测试库名）· 61（物化时注入 `QF_DATABASE_URL`）· 75-89（`_ensure_database`：`import psycopg` + `DROP DATABASE ... WITH (FORCE)` + `CREATE DATABASE`）· 93（注入 `QF_DATABASE_URL`）· 105-120（`engine` fixture 的 `drop_all/create_all`，**与方言无关，可留**） |
| `api/app/config.py` | 40（`database_url` 默认值）· 41-43（连接池参数）· 108-109（`is_postgres`）· **124-126 有一份重复定义**（与 94-109 重） |
| `api/app/db.py` | 31（`pool_pre_ping` 注释）· 34-36（`if settings.is_postgres` 才设池参数）· 37（SQLite 需 `connect_args={"check_same_thread": False}`） |

---

## 汇总（按"改动点数"排序，核对用）

| 文件 | 点数 | 类别 |
|---|---|---|
| `routers/chat.py` | ~42 | A（+2 方言） |
| `routers/progress.py` | ~18 | A |
| `routers/auth.py` `deps.py` `security.py` | ~35 | A（整文件删） |
| `routers/mybank.py` / `ai.py` / `problem.py` / `knowledge.py` | 10 / 9 / 8 / 8 | A |
| `models.py` | 11 表 + 22 列 JSONB | A + B |
| `pipeline/dbstore.py` / `dbsync.py` | ~16 / ~14 | B |
| `pipeline/graph_build.py` / `bankfile.py` / `promote.py` / `fix.py` / `rework.py` / `drive.py` / `coverage.py` / `dispatch.py` / `graph_log.py` | 2–8 each | B |
| `sync_ops.py` / `outline.py` / `ai_gateway.py` | ~12 / 5 / 4 | B |
| `api/alembic/versions/*.py` | 12 个文件 | B |
| `theme/runtime/boot.js` / `api.js` / `login.html` / `landing.html` | 6 / 5 / 4 / 1 | A |
| `api/tests/conftest.py` / `config.py` / `db.py` | 7 / 5 / 3 | B |
