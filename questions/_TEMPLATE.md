---
# ===========================================================================
# 题目模板 —— 复制本文件到 questions/<学科>/<学科>-<四位编号>-<短横线描述>.md
# 也可以直接用脚手架生成：
#     python3 tools/new_question.py --topic cpp-stl-iterator --type single
#
# 字段速查
#   id          必填，全局唯一，kebab-case，建议以学科名开头（cpp-0001）
#   type        必填，single | multi | blank | short | problem
#   topic       必填，meta/topics.yaml 里**任意一层**的 key。
#               三级结构：学科 → 单元 → 知识点；题目优先挂到知识点上，
#               一时拿不准就先挂单元（如 cpp-stl），之后细化了再往下挪。
#               按上层筛选会自动包含其全部子孙。
#   difficulty  1..5，默认 3
#   source      出处（书名 / 链接），可选
#
# 不再需要 tags 与 chapter：分类交给主题树，掌握程度由做题记录自动得出。
# ===========================================================================
id: cpp-9999
type: single
topic: cpp-stl-iterator
difficulty: 2
source: "示例出处"
---

这里是**题面**，支持 Markdown：行内公式 $O(n\log n)$，行内代码 `std::vector`，
也可以放代码块：

```cpp
std::vector<int> v;
v.reserve(100);
```

## 选项
- A. 第一个选项
- B. 第二个选项
- C. 第三个选项

## 答案
B

## 解析
提交后展示的解析，同样支持 Markdown 与公式 $\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$。


<!-- ==========================================================================
以下三种题型的写法示例（保留在本文件中仅作参考，出题时按 type 取用对应小节）

────────────────────────────── 多选 type: multi ──────────────────────────────
## 选项
- A. 选项一
- B. 选项二
- C. 选项三

## 答案
A,C

────────────────────────────── 填空 type: blank ──────────────────────────────
「## 答案」里每行对应一个空；同一个空的多个可接受答案用 | 分隔；
以 ~ 开头的项按正则匹配（用于「任意写法都算对」的场景）。

## 答案
1000|1e3|~^10\^3$
O(n)

────────────────────────────── 简答 type: short ──────────────────────────────
「## 参考答案」给完整答案；「## 评分要点」给 AI 批改用的 rubric，
每行一条，会作为评分依据传给模型。不写「## 答案」小节。

## 参考答案
完整参考答案（可含公式与代码）。

## 评分要点
- 要点一
- 要点二
- 要点三
========================================================================== -->
